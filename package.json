{
  "name": "password-policy-wall-of-shame",
  "version": "1.0.0",
  "description": "",
  "main": "index.html",
  "dependencies": {
    "fomantic-ui": "^2.8.5"
  },
  "scripts": {
    "setup": "bundle && yarn && cd semantic && yarn gulp build && cd .. && bundle exec jekyll build",
    "update": "bundle update && yarn upgrade -L",
    "build": "bundle exec jekyll build",
    "serve": "bundle exec jekyll serve --incremental --baseurl ''",
    "watch": "bundle exec jekyll build --watch --incremental --baseurl ''",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "gulp": "^4.0.2"
  }
}
