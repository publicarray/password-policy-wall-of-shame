{
  "name": "password-policy-wall-of-shame",
  "version": "1.0.0",
  "description": "",
  "main": "index.html",
  "dependencies": {
    "semantic-ui": "^2.3.1"
  },
  "devDependencies": {},
  "scripts": {
    "setup": "bundle && yarn && cd semantic && yarn gulp build && cd .. && bundle exec jekyll build",
    "update": "bundle update && yarn upgrade --latest",
    "build": "bundle exec jekyll build",
    "serve": "bundle exec jekyll serve --incremental",
    "watch": "bundle exec jekyll build --watch --incremental",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC"
}
